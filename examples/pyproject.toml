[project]
name = "dir-example"
version = "0.0.0"
requires-python = ">=3.10"
dependencies = [
    "agntcy-dir",
    "httpx>=0.28.1",
]

[[tool.uv.index]]
url = "https://buf.build/gen/python"

[tool.uv.sources]
agntcy-dir = { path = "..", editable = true }
